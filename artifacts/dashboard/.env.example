# Amise MedFlow Dashboard — environment variables
# Copy to .env.local and fill in your values.
# .env.local is git-ignored and NEVER committed.

# Supabase project URL (e.g. https://abcdefgh.supabase.co)
VITE_SUPABASE_URL=

# Supabase anon/public JWT key (starts with eyJ, ~200+ chars).
# NOT the opaque sb_publishable_ format — supabase-js v2 rejects it.
# Find it at: Supabase Dashboard > Settings > API > anon public
VITE_SUPABASE_ANON_KEY=

# Optional: Sentry DSN for error monitoring (PHI-scrubbed — see src/lib/sentry.ts).
# VITE_SENTRY_DSN=

# Optional: idle auto sign-out in minutes (default 15, clamped 2–120; cannot be disabled).
# VITE_IDLE_TIMEOUT_MINUTES=15
