name: Run DB Migrations

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type "run" to confirm executing migrations on production'
        required: true

jobs:
  migrate:
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.confirm == 'run' }}
    env:
      PROJECT_REF: nornhfzfrlmfzaqmrzzp
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
    steps:
      - uses: actions/checkout@v7

      - name: Run base schema
        run: |
          QUERY=$(jq -Rs . < supabase-schema.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 1: Clinical records"
        run: |
          QUERY=$(jq -Rs . < supabase-clinical-records-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 2: Patient portal"
        run: |
          QUERY=$(jq -Rs . < supabase-patient-portal-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 3: APCQ questionnaires"
        run: |
          QUERY=$(jq -Rs . < supabase-apcq-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 4: Questionnaire token expiry"
        run: |
          QUERY=$(jq -Rs . < supabase-questionnaire-token-expiry-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 5: EMR persistence"
        run: |
          QUERY=$(jq -Rs . < supabase-emr-persistence-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 6: Portal self-service"
        run: |
          QUERY=$(jq -Rs . < supabase-portal-self-service-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 7: AI adaptive intake"
        run: |
          QUERY=$(jq -Rs . < supabase-ai-adaptive-intake-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 8: Booking notifications"
        run: |
          QUERY=$(jq -Rs . < supabase-booking-notifications-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 9: Booking source"
        run: |
          QUERY=$(jq -Rs . < supabase-booking-source-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 10: Email document intake"
        run: |
          QUERY=$(jq -Rs . < supabase-email-document-intake-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 11: Document AI extraction"
        run: |
          QUERY=$(jq -Rs . < supabase-document-ai-extraction-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 12: Documents clinical photo"
        run: |
          QUERY=$(jq -Rs . < supabase-documents-clinical-photo-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 13: Vitals photo"
        run: |
          QUERY=$(jq -Rs . < supabase-vitals-photo-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 14: Service role grants"
        run: |
          QUERY=$(jq -Rs . < supabase-service-role-grants-fix-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 15: Appointment email optional"
        run: |
          QUERY=$(jq -Rs . < supabase-appointment-requests-email-optional-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 16: Referring doctors seed"
        run: |
          QUERY=$(jq -Rs . < supabase-referring-doctors-seed-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 17: Confirmed appointments"
        run: |
          QUERY=$(jq -Rs . < supabase-confirmed-appointments-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 18: Intake reminder"
        run: |
          QUERY=$(jq -Rs . < supabase-intake-reminder-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 19: Booking waitlist"
        run: |
          QUERY=$(jq -Rs . < supabase-booking-waitlist-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 20: Patients quarter"
        run: |
          QUERY=$(jq -Rs . < supabase-patients-quarter-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 21: Procedure prep drafts"
        run: |
          QUERY=$(jq -Rs . < supabase-procedure-prep-drafts-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 22: Post-visit followup"
        run: |
          QUERY=$(jq -Rs . < supabase-post-visit-followup-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 23: Appointment change requests"
        run: |
          QUERY=$(jq -Rs . < supabase-appointment-change-requests-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 24: User default site"
        run: |
          QUERY=$(jq -Rs . < supabase-user-default-site-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 25: Preferred slot text"
        run: |
          QUERY=$(jq -Rs . < supabase-preferred-slot-text-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 26: Staff review timestamps"
        run: |
          QUERY=$(jq -Rs . < supabase-staff-review-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 27: Anonymous intake support"
        run: |
          QUERY=$(jq -Rs . < supabase-anon-intake-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 28: Appointment questionnaire link"
        run: |
          QUERY=$(jq -Rs . < supabase-appointment-questionnaire-link-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 29: Appointment requests align"
        run: |
          QUERY=$(jq -Rs . < supabase-appointment-requests-align-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 30: Web intake delivery method"
        run: |
          QUERY=$(jq -Rs . < supabase-web-intake-delivery-method-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 31: Clinical persistence (surgical history, ROS, prescriptions, trauma)"
        run: |
          QUERY=$(jq -Rs . < supabase-clinical-persistence-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 32: EMR enhancements (formulary, procedure catalog, ICD-10)"
        run: |
          QUERY=$(jq -Rs . < supabase-emr-enhancement-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 33: Performance indexes"
        run: |
          QUERY=$(jq -Rs . < supabase-performance-indexes-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 34: Patient auth accounts"
        run: |
          QUERY=$(jq -Rs . < patient-auth-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 35: Storage buckets"
        run: |
          QUERY=$(jq -Rs . < supabase-storage-buckets-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 36: Phase 2 schema fixes"
        run: |
          QUERY=$(jq -Rs . < supabase-phase2-schema-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 37: Phase 4 follow-up loop (referrals, orders, results)"
        run: |
          QUERY=$(jq -Rs . < supabase-phase4-followup-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 38: Phase 6 patient identity & entry pipeline hardening"
        run: |
          QUERY=$(jq -Rs . < supabase-phase6-identity-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 39: Phase 8 calendar event cache"
        run: |
          QUERY=$(jq -Rs . < supabase-phase8-calendar-cache-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 40: Call logs"
        run: |
          QUERY=$(jq -Rs . < supabase-call-logs-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 41: Call logs — missing columns used by the API server"
        run: |
          QUERY=$(jq -Rs . < supabase-call-logs-columns-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 42: Call logs — practice_line & patient auto-MRN"
        run: |
          QUERY=$(jq -Rs . < supabase-call-logs-add-practice-line.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 43: Call logs — voicemail & callback queue"
        run: |
          QUERY=$(jq -Rs . < supabase-calls-voicemail-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 44: Call recordings storage bucket"
        run: |
          QUERY=$(jq -Rs . < supabase-call-recordings-storage.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 45: Seam fixes"
        run: |
          QUERY=$(jq -Rs . < supabase-seam-fixes-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 46: Patient token blacklist"
        run: |
          QUERY=$(jq -Rs . < supabase-patient-token-blacklist-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 47: Patients full fields + photo storage bucket"
        run: |
          QUERY=$(jq -Rs . < supabase-patients-full-fields-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 48: Patient photo_url column"
        run: |
          QUERY=$(jq -Rs . < supabase-add-photo-url.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 49: Clinical scores (Tokyo/Ranson/BISAP/ASA/POSSUM/Child-Pugh/MELD/NEWS2)"
        run: |
          QUERY=$(jq -Rs . < supabase-clinical-scores-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 50: Dual-confirmation booking gate"
        run: |
          QUERY=$(jq -Rs . < supabase-dual-confirmation-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 51: Local folder document source"
        run: |
          QUERY=$(jq -Rs . < supabase-local-folder-source-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 52: Results inbox fix"
        run: |
          QUERY=$(jq -Rs . < supabase-results-inbox-fix-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 53: Revoke anon access to appointment_requests"
        run: |
          QUERY=$(jq -Rs . < supabase-revoke-anon-appointment-requests-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 54: Slice A — AI proposals + note versioning"
        run: |
          QUERY=$(jq -Rs . < supabase-slice-a-ai-proposals-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 55: Slice B — workflow tasks"
        run: |
          QUERY=$(jq -Rs . < supabase-slice-b-workflow-tasks-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 56: Slice C — clinical state engine"
        run: |
          QUERY=$(jq -Rs . < supabase-slice-c-clinical-states-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 57: Slice F — loop closure"
        run: |
          QUERY=$(jq -Rs . < supabase-slice-f-loop-closure-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 58: Slice H — referral lifecycle"
        run: |
          QUERY=$(jq -Rs . < supabase-slice-h-referral-lifecycle-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 59: Slices L/M/N/O — encounter, notes, search, billing APIs"
        run: |
          QUERY=$(jq -Rs . < supabase-slice-lmno-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 60: Slices P/Q/R/S — API routes only (no schema changes)"
        run: |
          QUERY=$(jq -Rs . < supabase-slice-pqrs-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 61: Staff scheduling module"
        run: |
          QUERY=$(jq -Rs . < supabase-staff-scheduling-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 62: Tax planner state"
        run: |
          QUERY=$(jq -Rs . < supabase-tax-planner-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 63: Theatre sessions + cases"
        run: |
          QUERY=$(jq -Rs . < supabase-theatre-sessions-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 64: Encounters closed_at (reopen-for-edit grace period)"
        run: |
          QUERY=$(jq -Rs . < supabase-encounters-closed-at-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 65: Remove 'castries' as a valid site"
        run: |
          QUERY=$(jq -Rs . < supabase-remove-castries-site-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 66: Clinical audit-log trigger (dashboard writes)"
        run: |
          QUERY=$(jq -Rs . < supabase-clinical-audit-trigger-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 67: Atomic replace-all RPCs (medications/surgical_history/toxic_habits/operative_notes sync)"
        run: |
          QUERY=$(jq -Rs . < supabase-atomic-replace-all-sync-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 68: iOS EMR — working_diagnosis + assessment fields on patients; prescriptions + patient_documents tables"
        run: |
          QUERY=$(jq -Rs . < supabase-ios-emr-columns-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 69: iOS Consultation form — HPI, surgical hx, allergies, per-system exam, management plan columns on patients"
        run: |
          QUERY=$(jq -Rs . < supabase-ios-consultation-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 70: patients — pmh_notes + family_history_notes columns"
        run: |
          QUERY=$(jq -Rs . < supabase-patients-pmh-family-history-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 71: patients — setting, location, acuity columns for iOS multi-device sync"
        run: |
          QUERY=$(jq -Rs . < supabase-ios-patient-setting-sync-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 72: patients — clinical consultation fields (iOS ConsultationView / AssessmentView)"
        run: |
          QUERY=$(jq -Rs . < supabase-patient-clinical-fields-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 73: patients — physical exam fields (iOS per-system exam sync)"
        run: |
          QUERY=$(jq -Rs . < supabase-patient-exam-fields-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 74: patient_vitals — encounter-free vitals table for iOS VitalsEntry sync"
        run: |
          QUERY=$(jq -Rs . < supabase-patient-vitals-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 75: patient_operative_plans — consent, anaesthesia prep, WHO surgical checklist (JSONB)"
        run: |
          QUERY=$(jq -Rs . < supabase-patient-operative-plans-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 76: patient_billing_items — CPT-coded billing items for iOS BillingLineItem sync"
        run: |
          QUERY=$(jq -Rs . < supabase-patient-billing-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 77: prescriptions — add iOS flat-column schema (drug, dose, route, frequency, duration, indication, instructions, prescribed_at)"
        run: |
          QUERY=$(jq -Rs . < supabase-prescriptions-drug-columns-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 78: prescriptions — re-apply flat-column additions without backfill UPDATE (fixes migration 77 rollback)"
        run: |
          QUERY=$(jq -Rs . < supabase-prescriptions-flat-columns-fix-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 79: patient_documents — iOS document metadata table"
        run: |
          QUERY=$(jq -Rs . < supabase-patient-documents-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 80: mm_cases — RCA / postmortem analysis columns"
        run: |
          QUERY=$(jq -Rs . < supabase-mm-cases-rca-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 77: prescriptions — add iOS flat-column schema (drug, dose, route, frequency, duration, indication, instructions, prescribed_at)"
        run: |
          QUERY=$(jq -Rs . < supabase-prescriptions-drug-columns-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: "Migration 81: patients — add encounter_status and check_in_time for iOS front-desk handoff"
        run: |
          QUERY=$(jq -Rs . < supabase-encounter-status-migration.sql)
          curl -sf -X POST "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"query\": $QUERY}" | jq -e 'if type == "array" then . else error(.message // "SQL error") end'

      - name: Done
        run: echo "All 81 migrations completed successfully. See migrations/README.md for files intentionally excluded (conflicting duplicate table definitions) and how to add the next migration."
